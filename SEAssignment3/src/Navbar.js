const Navbar = ()=>{

    return(
        <nav className="navbar">
            <div className="links">
                <a href="#">Home</a>
                <a href="#">Schedule Tests</a>
                <a href="#">Contact</a>
            </div>
        </nav>

    );
}

export default Navbar;